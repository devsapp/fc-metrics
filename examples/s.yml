edition: 1.0.0          #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: fc-metricsApp     #  项目名称
access: aliyun-release  #  秘钥别名

services:
  fc-metrics-test: #  服务名称
    component: fc-metrics  # 组件名称
    props: #  组件的属性值
      regionId: cn-hangzhou
      serviceName:  test
      functionName:  testHello
